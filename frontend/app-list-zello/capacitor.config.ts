import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'App List - Zello',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
